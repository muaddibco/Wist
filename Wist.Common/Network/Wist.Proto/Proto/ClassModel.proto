syntax = "proto3";
package Wist.Proto.Model;

service SyncManager {
	rpc GetLastSyncBlock(Empty) returns (LastSyncBlock) {}
}

message Empty {}

message LastSyncBlock {
	uint64 Height = 1;
	bytes Hash = 2;
}
